name: simple_logger_package
description: A simple and lightweight logger for Flutter applications.
version: 0.0.1

repository: https://github.com/Hari-Baskar/simple_logger
issue_tracker: https://github.com/Hari-Baskar/simple_logger/issues

environment:
  sdk: ^3.9.2
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter: